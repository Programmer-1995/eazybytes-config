spring:
  application:
    name: account-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:mysql://localhost:2323/accounts_db
    username: ${DB_USER_NAME:root}
    password: ${DB_PASSWORD:Root@3064016837}
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8080

build:
  version: "3.0"

accounts:
  message: "Welcome to EazyBank accounts related local APIs"
  contactDetails:
    name: "John Doe- Developer"
    email: "john@eazybank.com"
  onCallSupport:
    - (412) 419-3491
    - (915) 382-1932
